# Copy to .env and set values. Required for live extraction (optional when DRY_RUN=true).

# Gemini (CrewAI native). Required unless DRY_RUN=true.
GEMINI_API_KEY=AIzaSyBQokElllSyWnQL5IQmD7voi6RYMWH701A

# Optional: gemini-2.5-flash (default), gemini-2.5-pro, etc. (gemini-2.0-flash deprecated for new users)
# GEMINI_MODEL=gemini-2.5-flash

# Mock mode: no API calls, deterministic mock JSON. Use for pipeline testing without billing.
# DRY_RUN=true
